import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    item: {
        padding: 10,
        borderBottomWidth: 1,
        borderColor: '#ddd',
    },

    name: {
        fontWeight: 'bold',
        color: '#2e7d32',
    },

    calories: {
        color: '#555',
    },
});