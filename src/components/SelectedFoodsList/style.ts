import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginTop: 10
    },

    title: {
        fontWeight: 'bold',
        color: '#2e7d32',
        marginBottom: 5
    },

    item: {
        color: '#555'
    },
});