import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    paddingVertical: 25,
    paddingHorizontal: 25,
    backgroundColor: "#fff",
    width: 380,
    marginTop: 40,
    borderRadius: 30
   
  },

  inputContainer: {
    gap: 40,
    marginBottom: 30
  },

    input: {
    borderBottomWidth: 2,
    borderColor: 'gray'
  },

  button: {
    alignItems: 'center',
    marginTop: 10
  }
});
