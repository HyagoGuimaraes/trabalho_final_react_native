import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#ffffff",
    padding: 16,
  },

  buttonNew: {
    backgroundColor: "#04dd04",
    padding: 12,
    borderRadius: 8,
    marginBottom: 12,
    alignItems: "center",
  },

  buttonText: {
    color: "#fff",
    fontWeight: "bold",
    fontSize: 16,
  },

  post: {
    padding: 15,
    borderBottomWidth: 1,
    borderColor: "#ddd",
  },

  postText: {
    fontSize: 16,
    color: "#333",
  },

  date: {
    fontSize: 12,
    color: "#888",
    marginTop: 4,
  },
});
